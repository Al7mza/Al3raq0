# Alternative requirements for Python 3.13+
# Use this if the main requirements.txt fails

# Core Telegram Bot
python-telegram-bot>=20.0

# Image Processing (headless versions avoid GUI dependencies)
Pillow>=10.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0

# OCR
pytesseract>=0.3.10

# Modern text matching (replaces fuzzywuzzy)
rapidfuzz>=3.0.0

# Configuration
python-dotenv>=1.0.0

# Alternative text matching libraries
difflib
re