# Robust Bot Requirements - Minimal dependencies for production hosting
# These packages are essential and avoid problematic builds

# Core Telegram Bot
python-telegram-bot>=20.0

# Configuration
python-dotenv>=0.19.0

# HTTP requests for monitoring
requests>=2.25.0

# Optional: For better performance (install manually if needed)
# numpy>=1.21.0
# Pillow>=9.0.0